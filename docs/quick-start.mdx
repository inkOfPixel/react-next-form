---
title: Quick start
slug: /
hide_table_of_contents: true
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

React Next Form is a lightweight React library for form state management. It eliminates a lot of boilerplate code, and less code means fewer bugs and things to worry about.

## Installation

React Next Form relies on [immer](https://immerjs.github.io/immer/docs/introduction) for immutable updates and [yup](https://github.com/jquense/yup) for schema validation.

<Tabs
  defaultValue="yarn"
  values={[
    {label: "Yarn", value: "yarn"},
    {label: "NPM", value: "npm"},
  ]}>
  <TabItem value="yarn">


```bash
yarn add -E react-next-form immer yup
```

  </TabItem>
  <TabItem value="npm">


```bash
npm i --save react-next-form immer yup
```

  </TabItem>
</Tabs>


## Basic form

<Tabs
  defaultValue="js"
  values={[
    {label: "Javascript", value: "js"},
    {label: "Typescript", value: "ts"},
  ]}>
  <TabItem value="js">


```jsx
import React from "react";
import { useForm } from "react-next-form";

export default function BasicForm() {
  const form = useForm({
    initialValues: { name: "" },
    onSubmit: values =>  alert(values.name);
  });

  return (
    <div>
      <input placeholder="type your name" {...form.fieldProps("name")} />
      <button onClick={form.submit}>Submit</button>
    </div>
  );
}
```

  </TabItem>
  <TabItem value="ts">


```tsx
import React from "react";
import { useForm } from "react-next-form";

interface Values {
  name: string;
}

export default function BasicForm() {
  const form = useForm<Values>({
    initialValues: { name: "" },
    onSubmit: (values) => alert(values.name),
  });

  return (
    <div>
      <input placeholder="type your name" {...form.fieldProps("name")} />
      <button onClick={form.submit}>Submit</button>
    </div>
  );
}
```

  </TabItem>
</Tabs>


[![Edit React Next Form - Basic form](https://codesandbox.io/static/img/play-codesandbox.svg)](https://codesandbox.io/s/react-next-form-basic-form-lqzsl?fontsize=14&hidenavigation=1&module=%2Fsrc%2FBasicForm.tsx&theme=dark)
